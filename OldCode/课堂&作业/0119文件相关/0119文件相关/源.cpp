#include<iostream>
#include<conio.h>
using namespace std;
#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#include<windows.h>
char *p;
int main(int argc,char *argv[])
{
	/*FILE *fp1=fopen("lower.txt","rb");
	clock_t begin=clock();
	Sleep(100);
	clock_t end=clock();
	cout<<(double)(end-begin)/CLOCKS_PER_SEC<<" secs\n";
	char buf[513]="";
	while(fgets(buf,512,fp1))
	{
		Sleep(500);
		//rewind(fp1);
		fseek(fp1,-500,SEEK_END);
		printf("%s\n",buf);
	}*/
	void switchoperator(void*(),void(*b)())
	{
		switch
			case:
				return b;
	}

	char ch;
	while((ch=	getch())!=EOF)
	{
		putchar('*');
	}


	return 0;
}
